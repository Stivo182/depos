// BSLLS:LineLength-off
// BSLLS:MagicNumber-off
// BSLLS:DuplicateStringLiteral-off

#Использовать "helpers"
#Использовать logos

Перем _Поделка; // Поделка
Перем _ПотокВывода; // ПотокВПамяти
Перем _ТекущийКаталог; // Строка
Перем _ПеременныеСредыКонфигурация; // Строка
Перем _Лог; // Лог
Перем _СообщенияОтладочногоЛога; // Массив

&ТестовыйНабор(Характер = "Одиночка")
Процедура ПриСозданииОбъекта()
КонецПроцедуры

&ПередВсеми
Процедура Инициализация() Экспорт
	_ТекущийКаталог = ТекущийКаталог();
	_ПеременныеСредыКонфигурация = ПолучитьПеременнуюСреды("OSCRIPT_CONFIG");
КонецПроцедуры

&ПередКаждым
Процедура ПередКаждым() Экспорт
	_Лог = Неопределено;
	_СообщенияОтладочногоЛога = Новый Массив();
КонецПроцедуры

&ПослеКаждого
Процедура ПослеКаждого() Экспорт
	Если _Лог <> Неопределено Тогда
		_Лог.УдалитьСпособВывода(ЭтотОбъект);
		Логирование.ЗакрытьЛог(_Лог);
		_Лог = Неопределено;
	КонецЕсли;
	ПомощникТестирования.УдалитьВременныеФайлы();
	УстановитьТекущийКаталог(_ТекущийКаталог);
	УстановитьПеременнуюСреды("OSCRIPT_CONFIG", _ПеременныеСредыКонфигурация);
КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиДиагностикуОшибкиНенайденногоМанифеста() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйПользовательскийВывод = "manifest.not_found: В каталоге";

	УстановитьТекущийКаталог("./tests");

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");
	ПодключитьСборОтладочногоЛога();

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);
	ТекстДиагностики = СтрСоединить(_СообщенияОтладочногоЛога, Символы.ПС);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйПользовательскийВывод);
	Ожидаем.Что(ТекстВывода)
		.Содержит("Убедитесь, что указан каталог, содержащий packagedef или opm-metadata.xml.");
	Ожидаем.Что(ТекстДиагностики).Содержит("Место:");

КонецПроцедуры

Процедура ПодключитьСборОтладочногоЛога()
	_Лог = Логирование.ПолучитьЛог("oscript.lib.depos.КомандаDoctor");
	_Лог.ДобавитьСпособВывода(ЭтотОбъект, УровниЛога.Отладка);
КонецПроцедуры

// Сохраняет отладочное сообщение временного appender'а.
//
// Параметры:
//   Сообщение - Строка - Форматированное сообщение.
//   УровеньСообщения - Число - Уровень сообщения logos.
Процедура Вывести(Сообщение, УровеньСообщения) Экспорт
	Если УровеньСообщения = УровниЛога.Отладка Тогда
		_СообщенияОтладочногоЛога.Добавить(Сообщение);
	КонецЕсли;
КонецПроцедуры

// Завершает работу временного appender'а.
Процедура Закрыть() Экспорт
КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьРезультатаАнализаМанифестаВТекущемКаталоге() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод = ОжидаемыйВыводРезультатаАнализа();

	УстановитьТекущийКаталог("./tests/fixtures/ПроектДляАнализа");

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть1);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть2);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть3);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьРезультатаАнализаМанифестаВУказанномКаталоге() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод = ОжидаемыйВыводРезультатаАнализа();

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть1);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть2);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть3);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьРезультатаАнализаУказанногоМанифеста() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод = ОжидаемыйВыводРезультатаАнализа();

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа/packagedef");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть1);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть2);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть3);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьРезультатаАнализаМанифестаСУказаннымиКаталогамиИсходников() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод = ОжидаемыйВыводРезультатаАнализа();

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа/packagedef");
	ПараметрыКоманды.Добавить("--src-dirs");
	ПараметрыКоманды.Добавить(ФС.ПолныйПуть("./tests/fixtures/ПроектДляАнализа/src"));
	ПараметрыКоманды.Добавить("--dev-dirs");
	ПараметрыКоманды.Добавить(ФС.ПолныйПуть("./tests/fixtures/ПроектДляАнализа/tests"));

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть1);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть2);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод.Часть3);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПроверитьИгнорированиеЗависимостейДляРазработки() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод1 = 
	"Найдено проблем: 8
	|  4 отсутствуют в манифесте
	|  2 не используются
	|  1 не установлен
	|  1 конфликт версий
	|
	|[1] Отсутствуют в манифесте
	|
	|   missing_manifests_present_src
	|      └─ %1\src\Классы\Класс1.os
	|   missing_manifests_present_src_tests
	|      ├─ %1\src\Классы\Класс1.os
	|      └─ %1\src\Классы\Класс2.os
	|   present_in_dev_src_missing_tests
	|      └─ %1\src\Классы\Класс1.os
	|   present_in_dev_src_tests
	|      └─ %1\src\Классы\Класс1.os
	|
	|[2] Объявлены, но не используются
	|
	|   present_in_prod_missing_src_tests
	|   present_in_prod_tests_missing_src
	|
	|[3] Не установлены
	|
	|   present_in_prod_missing_src_tests
	|
	|[4] Несоответствие версий";

	ОжидаемыйВывод2 =
	"[4] Несоответствие версий
	|
	|   present_in_prod_tests_missing_src
	|     установлена: 1.0.0
	|     требуется:   >=1.1.0
	|     путь:        %1\oscript_modules\present_in_prod_tests_missing_src";

	ПодготовитьОжидаемыйВывод(ОжидаемыйВывод1);
	ПодготовитьОжидаемыйВывод(ОжидаемыйВывод2);

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа/packagedef");
	ПараметрыКоманды.Добавить("--ignore-dev");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод1);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод2);
	Ожидаем.Что(ТекстВывода).Содержит("Найдено проблем: 8");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьРезультатаАнализаМанифестаПоИмениПакета() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	УстановитьТекущийКаталог("./tests/fixtures");
	ПомощникТестирования.УстановитьИзолированныеКаталогиБиблиотек(
		ФС.ПолныйПуть(ОбъединитьПути(_ТекущийКаталог, "tests/fixtures/oscript_modules")),
		"./nonexistent"
	);

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--package");
	ПараметрыКоманды.Добавить("mypackage");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит("[1] Объявлены, но не используются");
	Ожидаем.Что(ТекстВывода).Содержит("Несоответствие версий");
	Ожидаем.Что(ТекстВывода).Не_().Содержит("   1testrunner (dev)");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ИсключитьКонфликтыВерсийЗависимостейДляРазработкиПриПроверкеПакетаПоИмени() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	УстановитьТекущийКаталог("./tests/fixtures");
	ПомощникТестирования.УстановитьИзолированныеКаталогиБиблиотек(
		ФС.ПолныйПуть(ОбъединитьПути(_ТекущийКаталог, "tests/fixtures/oscript_modules")),
		"./nonexistent"
	);

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--package");
	ПараметрыКоманды.Добавить("mypackage");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит("   annotations");
	Ожидаем.Что(ТекстВывода).Содержит("     установлена: 1.0.0");
	Ожидаем.Что(ТекстВывода).Содержит("     требуется:   >=1.3.0");
	Ожидаем.Что(ТекстВывода).Не_().Содержит("   1testrunner (dev)");
	Ожидаем.Что(ТекстВывода).Не_().Содержит("     требуется:   >=1.8.0");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПолучитьОшибкуПриПередачеКлючейМанифестаИИмениПакета() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВывод = "Ключи -m (--manifest) и -p (--package) нельзя использовать одновременно";

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/packagedef");
	ПараметрыКоманды.Добавить("--package");
	ПараметрыКоманды.Добавить("autumn");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Утверждение
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВывод);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ИсключитьПакетыИзАнализаПоФильтру() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа/packagedef");
	ПараметрыКоманды.Добавить("-x");
	ПараметрыКоманды.Добавить("present_in_dev_src_tests");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Утверждение
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Не_().Содержит("present_in_dev_src_tests");
	Ожидаем.Что(ТекстВывода).Содержит("present_in_prod_tests_missing_src");
	Ожидаем.Что(ТекстВывода).Содержит("Найдено проблем: 13");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиКонфликтыТранзитивныхЗависимостей() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	ОжидаемыйВыводНесовместимыеТребования =
	"     причина:     несовместимые требования
	|     установлена: 1.5.0
	|     требуется:   >=2.0.0; <=1.0.0
	|     источники:   root_a, root_b
	|     цепочки:
	|       ├─ root_a -> shared
	|       └─ root_b -> shared";

	ОжидаемыйВыводНеустановленнаяЗависимость =
	"   missing_shared
	|     причина:     транзитивная зависимость не установлена
	|     требуется:   >=1.0.0
	|     источник:    root_missing
	|     цепочка:     root_missing -> missing_shared";

	ОжидаемыйВыводНесоответствиеRootA =
	"     источник:    root_a
	|     цепочка:     root_a -> shared";

	ОжидаемыйВыводНесоответствиеRootB =
	"     источник:    root_b
	|     цепочка:     root_b -> shared";

	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектСТранзитивнымиКонфликтами/packagedef");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит("[1] Объявлены, но не используются");
	Ожидаем.Что(ТекстВывода).Содержит("[2] Конфликты транзитивных зависимостей");
	Ожидаем.Что(ТекстВывода).Содержит("4 конфликта транзитивных зависимостей");
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВыводНесовместимыеТребования);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВыводНеустановленнаяЗависимость);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВыводНесоответствиеRootA);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйВыводНесоответствиеRootB);
	Ожидаем.Что(ТекстВывода).Содержит("Найдено проблем: 7");

КонецПроцедуры

&Тест
Процедура ТестДолжен_НеДублироватьОдинаковыйТранзитивныйКонфликтВВыводе() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектСТранзитивнымиКонфликтами/packagedef-duplicate-dev");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);

	Ожидаем.Что(ТекстВывода).Содержит("1 конфликт транзитивных зависимостей");
	Ожидаем.Что(КоличествоВхождений(ТекстВывода, "   duplicate_leaf (dev)")).Равно(1);
	Ожидаем.Что(ТекстВывода).Содержит("цепочка:     duplicate_parent -> duplicate_leaf");

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("usage", "Найдено проблем: 10", " не установлен")
&ИсточникЗначение("installed", "Найдено проблем: 5", " не используется")
&ИсточникЗначение("transitive", "Проблем не найдено", "Обнаружены проблемы")
Процедура ТестДолжен_ВыполнитьВыбранныеПроверки(Проверки, ОжидаемыйТекст, НеожидаемыйТекст) Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектДляАнализа/packagedef");
	ПараметрыКоманды.Добавить("--checks");
	ПараметрыКоманды.Добавить(Проверки);

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);
	Ожидаем.Что(ТекстВывода).Содержит(ОжидаемыйТекст);
	Ожидаем.Что(ТекстВывода).Не_().Содержит(НеожидаемыйТекст);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиОшибкуНеизвестнойПроверки() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--checks");
	ПараметрыКоманды.Добавить("usage,unknown");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);
	Ожидаем.Что(ТекстВывода).Содержит("checks.invalid: Неизвестная проверка зависимостей 'unknown'");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ИсключитьПакетДоТранзитивногоАнализа() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектСИсключеннойПоврежденнойЗависимостью/packagedef");
	ПараметрыКоманды.Добавить("--exclude");
	ПараметрыКоманды.Добавить("broken-root");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);
	Ожидаем.Что(ТекстВывода).Содержит("Проблем не найдено");
	Ожидаем.Что(ТекстВывода).Не_().Содержит("dependency.version_invalid");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиПоврежденныеУстановленныеПакеты() Экспорт

	// Подготовка
	_ПотокВывода = ПомощникТестирования.ПерехватитьПотокВывода();
	_Поделка = ПомощникТестирования.ОтладочнаяПоделка();
	КонсольноеПриложение = _Поделка.НайтиЖелудь("КонсольноеПриложение");

	ПараметрыКоманды = Новый Массив();
	ПараметрыКоманды.Добавить("doctor");
	ПараметрыКоманды.Добавить("--manifest");
	ПараметрыКоманды.Добавить("./tests/fixtures/ПроектСПоврежденнымиПакетами/packagedef");
	ПараметрыКоманды.Добавить("--checks");
	ПараметрыКоманды.Добавить("installed");

	// Действие
	КонсольноеПриложение.Запустить(ПараметрыКоманды);

	// Проверка
	ТекстВывода = ПомощникТестирования.ПолучитьТекстИзПотокаВывода(_ПотокВывода);
	Ожидаем.Что(ТекстВывода).Содержит("Найдено проблем: 3");
	Ожидаем.Что(ТекстВывода).Содержит("3 поврежденных установленных пакета");
	Ожидаем.Что(ТекстВывода).Содержит("Повреждены установленные пакеты");
	Ожидаем.Что(ТекстВывода).Содержит("missing-metadata");
	Ожидаем.Что(ТекстВывода).Не_().Содержит("empty-version");
	Ожидаем.Что(ТекстВывода).Содержит("invalid-version");
	Ожидаем.Что(ТекстВывода).Содержит("invalid-xml");
	Ожидаем.Что(ТекстВывода).Содержит("причина:");
	Ожидаем.Что(ТекстВывода).Содержит("путь:");

КонецПроцедуры

Функция ОжидаемыйВыводРезультатаАнализа()
	
	ОжидаемыйВывод = Новый Структура("Часть1, Часть2, Часть3");

	ОжидаемыйВывод.Часть1 = 
	"Найдено проблем: 15
	|  6 отсутствуют в манифесте
	|  4 не используются
	|  3 не установлены
	|  2 конфликта версий
	|
	|[1] Отсутствуют в манифесте
	|
	|   missing_manifests_present_src
	|      └─ %1\src\Классы\Класс1.os
	|   missing_manifests_present_src_tests
	|      ├─ %1\src\Классы\Класс1.os
	|      └─ %1\src\Классы\Класс2.os
	|   missing_manifests_present_src_tests (dev)
	|      └─ %1\tests\Тесты.os
	|   missing_manifests_present_tests (dev)
	|      └─ %1\tests\Тесты.os
	|   present_in_dev_src_missing_tests
	|      └─ %1\src\Классы\Класс1.os
	|   present_in_dev_src_tests
	|      └─ %1\src\Классы\Класс1.os
	|
	|[2] Объявлены, но не используются
	|
	|   present_in_dev_missing_src_tests (dev)
	|   present_in_dev_src_missing_tests (dev)
	|   present_in_prod_missing_src_tests
	|   present_in_prod_tests_missing_src
	|
	|[3] Не установлены
	|
	|   present_in_dev_missing_src_tests (dev)
	|   present_in_dev_tests_missing_src (dev)
	|   present_in_prod_missing_src_tests
	|
	|[4] Несоответствие версий";

	ОжидаемыйВывод.Часть2 = 
	"   present_in_dev_src_tests (dev)
	|     установлена: 1.0.0
	|     требуется:   >=0.1.0,<=0.9.0
	|     путь:        %1\oscript_modules\present_in_dev_src_tests
	|
	|   present_in_prod_tests_missing_src
	|     установлена: 1.0.0
	|     требуется:   >=1.1.0
	|     путь:        %1\oscript_modules\present_in_prod_tests_missing_src";

	ОжидаемыйВывод.Часть3 = "Обнаружены проблемы с зависимостями.";

	ПодготовитьОжидаемыйВывод(ОжидаемыйВывод.Часть1);
	ПодготовитьОжидаемыйВывод(ОжидаемыйВывод.Часть2);

	Возврат ОжидаемыйВывод;

КонецФункции

Функция КоличествоВхождений(Текст, Подстрока)

	Количество = 0;
	Позиция = СтрНайти(Текст, Подстрока);

	Пока Позиция > 0 Цикл
		Количество = Количество + 1;
		СледующаяПозиция = Позиция + СтрДлина(Подстрока);
		ОстатокТекста = Сред(Текст, СледующаяПозиция);
		ПозицияВОстатке = СтрНайти(ОстатокТекста, Подстрока);

		Если ПозицияВОстатке = 0 Тогда
			Прервать;
		КонецЕсли;

		Позиция = СледующаяПозиция + ПозицияВОстатке - 1;
	КонецЦикла;

	Возврат Количество;

КонецФункции

Процедура ПодготовитьОжидаемыйВывод(ОжидаемыйВывод)

	ОжидаемыйВывод = СтрЗаменить(ОжидаемыйВывод, "\", ПолучитьРазделительПути());
	ОжидаемыйВывод = СтрШаблон(ОжидаемыйВывод, ФС.ПолныйПуть("./tests/fixtures/ПроектДляАнализа"));

КонецПроцедуры
