// BSLLS:LineLength-off
// BSLLS:MagicNumber-off
// BSLLS:DuplicateStringLiteral-off

#Использовать autumn
#Использовать asserts
#Использовать errors
#Использовать fs
#Использовать "../src/core"

Перем _Поделка; // Поделка

&ТестовыйНабор(Характер = "Одиночка")
Процедура ПриСозданииОбъекта()
КонецПроцедуры

&Тест
Процедура ТестДолжен_СообщитьОбОтсутствующемКаталогеИсходников() Экспорт

	// Подготовка
	Сканер = _Поделка.НайтиЖелудь("СканерИсходников");
	Параметры = Новый Массив();
	Параметры.Добавить("./tests/fixtures/НесуществующийКаталогИсходников");

	// Действие
	Ошибка = Неопределено;
	Попытка
		Сканер.НайтиПакеты(Параметры);
	Исключение
		Ошибка = ИнформацияОбОшибке();
	КонецПопытки;

	// Проверка
	Ожидаем.Что(Ошибка).Заполнено();
	Ожидаем.Что(ДиагностикаОшибок.КодОшибки(Ошибка)).Равно("source.directory_not_found");
	КадрОшибки = ДиагностикаОшибок.НайтиПервую(Ошибка, "source.directory_not_found");
	Ожидаем.Что(КадрОшибки.Данные().Каталог).Равно(Параметры[0]);

КонецПроцедуры

&ПередВсеми
Процедура Инициализация() Экспорт

	_Поделка = Новый Поделка();
	_Поделка.ЗапуститьПриложение();

КонецПроцедуры

&Тест
Процедура ТестДолжен_НайтиИспользованиеПакетовВИсходниках() Экспорт
	
	// Подготовка
	Сканер = _Поделка.НайтиЖелудь("СканерИсходников");
	
	// Действие
	Результат = Сканер.НайтиПакеты("./tests/fixtures/ПроектДляСканера");

	// Проверка
	Ожидаем.Что(Результат).ИмеетДлину(3);
	Ожидаем.Что(Результат[0].ИмяПакета).Равно("test1");
	Ожидаем.Что(ФС.ФайлСуществует(Результат[0].Файл)).ЭтоИстина();
	Ожидаем.Что(Результат[1].ИмяПакета).Равно("test2");
	Ожидаем.Что(ФС.ФайлСуществует(Результат[1].Файл)).ЭтоИстина();
	Ожидаем.Что(Результат[2].ИмяПакета).Равно("test3");
	Ожидаем.Что(ФС.ФайлСуществует(Результат[2].Файл)).ЭтоИстина();

КонецПроцедуры
