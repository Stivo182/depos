#Использовать semver

#Область ОписаниеПеременных

Перем _РегулярноеВыражениеИзвлеченияВерсии; // РегулярноеВыражение
Перем _РегулярноеВыражениеСтрогойВерсии; // РегулярноеВыражение

#КонецОбласти

#Область ОбработчикиСобытий

// Класс для парсинга HTML-страниц пакетов из реестра OneScript.
&Желудь
Процедура ПриСозданииОбъекта()
		
	_РегулярноеВыражениеИзвлеченияВерсии = Новый РегулярноеВыражение(
		"href=""\/download\/[a-zA-Z_0-9-]+\/[a-zA-Z_0-9-]+-([0-9A-Za-z+.-]+)\.ospx"""
	);

	ШаблонЧисловогоИдентификатора = "(0|[1-9][0-9]*)";
	ШаблонАлфавитноЧисловогоИдентификатора = "[0-9]*[A-Za-z-][0-9A-Za-z-]*";
	ШаблонИдентификатораПререлиза = "(" + ШаблонЧисловогоИдентификатора
		+ "|" + ШаблонАлфавитноЧисловогоИдентификатора + ")";
	ШаблонПререлиза = ШаблонИдентификатораПререлиза
		+ "(\." + ШаблонИдентификатораПререлиза + ")*";
	ШаблонМетаданных = "[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*";
	_РегулярноеВыражениеСтрогойВерсии = Новый РегулярноеВыражение(
		"^" + ШаблонЧисловогоИдентификатора + "\." + ШаблонЧисловогоИдентификатора
		+ "\." + ШаблонЧисловогоИдентификатора + "(-" + ШаблонПререлиза + ")?"
		+ "(\+" + ШаблонМетаданных + ")?$"
	);

КонецПроцедуры

#КонецОбласти

#Область ПрограммныйИнтерфейс

// Извлекает все доступные версии пакета из HTML-контента страницы.
//
// Параметры:
//   HTMLКонтент - Строка - HTML-код страницы пакета из реестра
//
// Возвращаемое значение:
//   Массив из Строка - Найденные версии пакета
Функция ИзвлечьВерсии(HTMLКонтент) Экспорт

	ВерсииПакета = Новый Массив();

	Совпадения = _РегулярноеВыражениеИзвлеченияВерсии.НайтиСовпадения(HTMLКонтент);

	Для Каждого Совпадение Из Совпадения Цикл
		ВерсияСтрокой = Совпадение.Группы[1].Значение;
		Если ЭтоКорректнаяВерсия(ВерсияСтрокой) Тогда
			ВерсииПакета.Добавить(ВерсияСтрокой);
		КонецЕсли;
	КонецЦикла;

	Возврат ВерсииПакета;

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ЭтоКорректнаяВерсия(ВерсияСтрокой)

	Если Не _РегулярноеВыражениеСтрогойВерсии.Совпадает(ВерсияСтрокой) Тогда
		Возврат Ложь;
	КонецЕсли;

	ВерсияОбъект = Новый Версия(ВерсияСтрокой);
	Возврат Не ВерсияОбъект.Ошибка();

КонецФункции

#КонецОбласти
